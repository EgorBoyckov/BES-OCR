"""Инструменты для разработки (не часть поставляемого приложения).

Ничего отсюда не импортируется из `core`/`gui`/`jobs` — офлайн-приложению
для работы пользователя эти модули не нужны, они нужны только при
разработке для объективной проверки визуального сходства результата с
оригиналом (см. `validate.py`).
"""
